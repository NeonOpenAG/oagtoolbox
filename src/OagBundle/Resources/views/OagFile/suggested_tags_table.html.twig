<table class="pure-table pure-table-bordered">
    <thead>
        <tr>
            <th>AGROVOC Code</th>
            <th>Description</th>
            <th>Confidence</th>
        </tr>
    </thead>
    <tbody>
        {% for tag in tags %}
            <tr>
                {{ include('OagBundle::OagFile/tag_row.html.twig', { 'tag': tag.tag }) }}
                <td>{{ tag.confidence }}</td>
            </tr>
        {% endfor %}
    </tbody>
</table>
