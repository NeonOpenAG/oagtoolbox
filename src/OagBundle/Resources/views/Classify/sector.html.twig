{% extends 'OagBundle::Default/index.html.twig' %}

{% block body %}

    {{ form_start(form) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}

    {% if xml is defined %}
      <pre>{{ xml }}</pre>
    {% endif %}

{% endblock %}
