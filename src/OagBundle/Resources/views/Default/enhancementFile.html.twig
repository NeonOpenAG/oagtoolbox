<a
    href="{{ path('oag_oagfile_enhancement', { 'id': enhancingDoc.id }) }}"
    {% if enhancingDoc.suggestedSectors | length > 0 %}
        title="{{ enhancingDoc.suggestedSectors | suggestedSectorList }}"
    {% endif %}
    >
    {{ enhancingDoc.documentName }}
</a>

[{{ enhancingDoc.suggestedSectors | length }}]